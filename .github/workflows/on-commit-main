name: on-commit-main

on:
  push:
    branches: *
     
jobs:    
  build:
    runs-on: ubuntu-latest
    name: on-commit-main
    steps:
     - name: deploy-main
       run: |
         echo "Deploying main"
         echo "${{ github.push.labels }}"
     
